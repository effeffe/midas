# MidasConfig.cmake

if (MIDAS_FOUND)
    return()
endif (MIDAS_FOUND)
set (MIDAS_FOUND TRUE)

set (ROOT_FOUND @ROOT_FOUND@)
if (ROOT_FOUND)
    include_directories(@ROOT_INCLUDES@)
endif (ROOT_FOUND)

# MIDAS_COMP_DEFS
# set_property (DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR} PROPERTY COMPILE_DEFINITIONS @MIDAS_COMP_DEFS@)

# Make customizations below

#set (MIDAS_VERSION_MAJOR @MIDAS_VERSION_MAJOR@)
#set (MIDAS_VERSION_MINOR @MIDAS_VERSION_MINOR@)
#set (MIDAS_REVISION @MIDAS_REVISION@)
#set (MIDAS_VERSION @MIDAS_VERSION@)
#set (MIDAS_FULL_VERSION @MIDAS_FULL_VERSION@)
set (MIDAS_PACKAGE_NAME midas )
#set (MIDAS_PACKAGE_STRING @MIDAS_PACKAGE_STRING@)

list (APPEND MIDAS_INCLUDE_DIRS @SYSINC_DIR@)
list (APPEND MIDAS_LIBRARY_DIRS @SYSLIB_DIR@)
list (APPEND MIDAS_LIBRARIES @MIDAS_LIBRARIES@)
